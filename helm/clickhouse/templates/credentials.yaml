apiVersion: v1
kind: Secret
metadata:
  name: clickhouse-credentials
type: Opaque
stringData:
  user: "{{ .Values.cluster.defaultUser.user }}"
  password: "{{ .Values.cluster.defaultUser.password }}"
