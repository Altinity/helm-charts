apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clickhouse.credentialsName" . }}
type: Opaque
stringData:
  user: "{{ .Values.defaultUser.user }}"
  password: "{{ .Values.defaultUser.password }}"
